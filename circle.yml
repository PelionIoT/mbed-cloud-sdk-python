machine:
  services:
    - docker

dependencies:
  pre:
    - |
      pip install awscli
      login="$(aws ecr get-login --no-include-email)"
      ${login}

test:
  override:
    - tox

deployment:
  production:
    branch: master
    commands:
      - pip install pandoc
      - ./generate-docs.sh
