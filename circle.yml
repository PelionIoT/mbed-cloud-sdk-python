machine:
  post:
    - pyenv global 2.7.10 3.4.3

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install pandoc

test:
  override:
    - flake8 --config ./.flake8
    - ./tests/run.sh

deployment:
  production:
    branch: master
    commands:
      - ./generate-docs.sh
