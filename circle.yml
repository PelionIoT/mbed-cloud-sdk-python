dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install pandoc

test:
  override:
    - flake8 --config ./.flake8
    - python -m unittest discover tests/

deployment:
  production:
    branch: master
    commands:
      - ./generate-docs.sh
