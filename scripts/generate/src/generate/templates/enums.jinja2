"""
Enum module

This file is autogenerated from api specifications
"""

# Python 2 compatibility
from __future__ import unicode_literals
from builtins import str
from builtins import object


class BaseEnum(object):
    values = frozenset()

    def __setattr__(self, name, value):
        raise Exception('enum container values cannot be modified')

{% for enum in enums %}

class {{ enum.enum_name.pascal }}(BaseEnum):
    """Represents the `{{enum.enum_name.pascal}}` options

    as used by Mbed Cloud "{{ enum.group_id.snake }}" functionality
    """

    {% for value in enum['values'] %}{{ value|upper }} = '{{ value }}'
    {% endfor %}

    values = frozenset((
        {% for value in enum['values'] %}'{{ value }}',
        {% endfor %}
    ))
{% endfor %}
